Flask==2.2.2
requests==2.28.1
Flask-SQLAlchemy==2.5.1
psycopg2-binary==2.9.3
flask-cors==3.0.10

Explanation
requirements.txt
    Flask: A micro web framework for Python.
        Flask==2.2.2: The version 2.2.2 of Flask.

    requests: A simple, yet elegant HTTP library for Python.
        requests==2.28.1: The version 2.28.1 of the requests library.

    Flask-SQLAlchemy: An extension for Flask that adds support for SQLAlchemy, a SQL toolkit and Object-Relational Mapping (ORM) library.
        Flask-SQLAlchemy==2.5.1: The version 2.5.1 of Flask-SQLAlchemy.

    psycopg2-binary: A PostgreSQL adapter for Python.
        psycopg2-binary==2.9.3: The version 2.9.3 of psycopg2-binary.

    flask-cors: A Flask extension for handling Cross-Origin Resource Sharing (CORS), making cross-origin AJAX possible.
        flask-cors==3.0.10: The version 3.0.10 of flask-cors.

Usage

To install these dependencies, run the following command in your project's root directory:

bash

pip install -r requirements.txt

This will install all the listed packages with their specified versions, ensuring that your development environment is consistent and matches the project's requirements.
